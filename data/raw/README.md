# Raw Data Inventory

This folder holds the raw inputs used by the notebooks.

Available raw inputs in this snapshot:

- `roadwork/dot_inhouse_resurfacing.csv`
- `weather/hourly/weather_hourly_1.csv` to `weather_hourly_6.csv`
- `weather/daily/weather_daily_1.csv` to `weather_daily_6.csv`
- `traffic/traffic_1.csv`
- `traffic/traffic_2.csv`

Raw inputs that are still incomplete:

- `plow/plow_0.csv` to `plow_39.csv` or the equivalent raw plow export set

Right now only `plow_0.csv` and `plow_1.csv` are present locally.

The notebooks point here for raw inputs.

The missing raw plow files were also not present in `GITHUB_NEWER/fall-2025-climate-and-road-maintenance-main`.
